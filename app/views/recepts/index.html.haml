- @recepts.each_slice(3) do |recept|
  .row
    - recept.each do |recept|
      .col-md-4
        .recept
          .image_wrapper
            = link_to recept do
              = image_tag recept.image.url(:medium)
          %h2= link_to recept.title, recept, data: { turbolinks: false }
